# Frontier Models Transition References

This directory contains model-specific reference examples.

It is not the primary entry point for current provider operations.

Use these folders when you need to:

- preserve an older example temporarily
- translate a model-pinned prototype into a current provider skill
- study a narrow model behavior without treating it as repository-wide default guidance

For active implementation work, start with `Skills/AI_Providers/`.
